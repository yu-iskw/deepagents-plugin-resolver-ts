apiVersion: deepagents.plugins/v2
kind: PluginSet

metadata:
  name: official-marketplace-example

marketplaces:
  - name: claude-plugins-official
    source:
      type: github
      repository: anthropics/claude-plugins-official
      ref: main

# Demo trust: development allows subagents so official agents/*.md compile.
# Production apps should prefer third-party-restricted + digest approvals.
plugins:
  - id: frontend-design@claude-plugins-official
    trustPolicy: &demoTrust development
    features: &officialFeatures
      skills: enabled
      commands: enabled
      syncSubagents: enabled
      mcp: disabled
      hooks: disabled
  - id: code-review@claude-plugins-official
    trustPolicy: *demoTrust
    features: *officialFeatures
  - id: commit-commands@claude-plugins-official
    trustPolicy: *demoTrust
    features: *officialFeatures
  - id: feature-dev@claude-plugins-official
    trustPolicy: *demoTrust
    features: *officialFeatures
  - id: pr-review-toolkit@claude-plugins-official
    trustPolicy: *demoTrust
    features: *officialFeatures

runtime:
  compatibilityMode: standard

output:
  directory: .deepagents/plugins
  reproducible: true
  includeSourceFiles: true
  emitCompatibilityReport: true
  emitSbom: false
